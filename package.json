{
    "name": "sites-by-colin",
    "private": true,
    "scripts": {
        "vercel-build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "serverless-http": "^3.2.0"
    }
}